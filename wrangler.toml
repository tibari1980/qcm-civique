name = "qcm-app"
pages_build_output_dir = ".vercel/output"
compatibility_date = "2024-04-03"
compatibility_flags = [ "nodejs_compat" ]

[vars]
# Configurez vos variables (BREVO_API_KEY, etc.) dans le Dashboard Cloudflare.
